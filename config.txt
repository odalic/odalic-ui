[server]
address=http://localhost:8080/odalic/

[requests]
timeout=60000

[signup]
; loginMode: gitlab (Gitlab OAuth tokens are used, users managed by Gitlab) | default (JWT tokens, users managed by Odalic)
loginMode=default
emailConfirmation=false
automaticLogin=true
; url of the Gitlab instance (main page). Used only if loginMode gitlab is turned on. 
gitlabUrl=
; Id (client Id) of the application authorized in gitlab. Used only if loginMode gitlab is turned on. 
gitlabApplicationId=
; Location where user is redirected after successfull auth by Gitlab. Used only if loginMode gitlab is turned on. 
gitlabApplicationRedirectUrl=

[alerts]
duration=5000
